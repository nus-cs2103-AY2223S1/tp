package swift.testutil;

import swift.commons.core.index.Index;

/**
 * A utility class containing a list of Task {@code Index} objects to be used in tests.
 */
public class TypicalTaskIndexes {
    public static final Index INDEX_FIRST_TASK = Index.fromOneBased(1);
    public static final Index INDEX_SECOND_TASK = Index.fromOneBased(2);
}
