package seedu.address.testutil;

import seedu.address.model.listing.ListingId;

/**
 * A utility class containing a list of {@code ListingId} objects to be used in tests.
 */
public class TypicalListingId {
    public static final ListingId ID_FIRST_LISTING = new ListingId("1");
    public static final ListingId ID_SECOND_LISTING = new ListingId("2");
    public static final ListingId ID_THIRD_LISTING = new ListingId("3");
}
