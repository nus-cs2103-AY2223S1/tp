package seedu.address.testutil;

import seedu.address.commons.core.index.Index;

/**
 * A utility class containing a list of {@code Index} objects to be used in tests.
 */
public class TypicalIndexes {
    public static final Index INDEX_FIRST_PROFILE = Index.fromOneBased(1);
    public static final Index INDEX_SECOND_PROFILE = Index.fromOneBased(2);
    public static final Index INDEX_THIRD_PROFILE = Index.fromOneBased(3);
    public static final Index INDEX_FIRST_EVENT = Index.fromOneBased(1);
    public static final Index INDEX_SECOND_EVENT = Index.fromOneBased(2);
    public static final Index INDEX_THIRD_EVENT = Index.fromOneBased(3);
}
