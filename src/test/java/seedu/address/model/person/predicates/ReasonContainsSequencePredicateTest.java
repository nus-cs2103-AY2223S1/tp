package seedu.address.model.person.predicates;

import static org.junit.jupiter.api.Assertions.*;

import org.junit.jupiter.api.Test;

import seedu.address.model.person.Appointment;
import seedu.address.model.person.Person;
import seedu.address.testutil.PersonBuilder;

class ReasonContainsSequencePredicateTest {

    /*
    @Test
    public void equals() {
        String firstSequence = "first";
        String secondSequence = "second";

        ReasonContainsSequencePredicateTest firstPredicate = new ReasonContainsSequencePredicateTest(firstSequence);
        ReasonContainsSequencePredicateTest firstPredicateCopy = new ReasonContainsSequencePredicateTest(firstSequence);
        ReasonContainsSequencePredicateTest secondPredicate = new ReasonContainsSequencePredicateTest(secondSequence);

        PredicateTestUtil.assertBasicEqualities(firstPredicate, firstPredicateCopy, secondPredicate);
    }

    private Appointment generateAppointmentWithName(String name) {
        return new PersonBuilder().withName(name).build();
    }

     */
}