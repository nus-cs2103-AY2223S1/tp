package seedu.rc4hdb.testutil;

import seedu.rc4hdb.commons.core.index.Index;

/**
 * A utility class containing a list of {@code Index} objects to be used in tests.
 */
public class TypicalIndexes {
    public static final Index INDEX_FIRST_RESIDENT = Index.fromOneBased(1);
    public static final Index INDEX_SECOND_RESIDENT = Index.fromOneBased(2);
    public static final Index INDEX_THIRD_RESIDENT = Index.fromOneBased(3);
}
