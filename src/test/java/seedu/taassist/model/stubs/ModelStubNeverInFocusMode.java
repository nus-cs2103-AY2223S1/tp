package seedu.taassist.model.stubs;

/**
 * A Model stub that will never be in focus mode.
 */
public class ModelStubNeverInFocusMode extends ModelStub {
    @Override
    public boolean isInFocusMode() {
        return false;
    }
}
