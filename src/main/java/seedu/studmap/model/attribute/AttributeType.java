package seedu.studmap.model.attribute;

/**
 * Enum class for valid attributes supported by sorting.
 */
public enum AttributeType {
    NAME,
    PHONE,
    ADDRESS,
    ID,
    GIT,
    HANDLE,
    EMAIL,
    ATTENDANCE
}
