package seedu.address.logic.commands;

/**
 * Represents all commands in the `reminder` command group.
 */
public abstract class ReminderCommandGroup extends Command {
    public static final String COMMAND_GROUP = "reminder";
}
