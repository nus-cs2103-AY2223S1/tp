package seedu.address.logic.commands;

/**
 * Adds a person or a task to the address book.
 */
public abstract class AddGenericCommand extends Command {
    public static final String COMMAND_WORD = "add";
}
