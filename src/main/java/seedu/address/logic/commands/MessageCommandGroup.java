package seedu.address.logic.commands;


/**
 * Represents all commands in the `message` command group.
 */
public abstract class MessageCommandGroup extends Command {
    public static final String COMMAND_GROUP = "message";
}
