package seedu.address.model;

/**
 * Represents different model type in ModQuik.
 */
public enum ModelType {
    ALL,
    STUDENT,
    TUTORIAL,
    CONSULTATION,
    GRADE_CHART,
    HELP,
    EXIT
}
