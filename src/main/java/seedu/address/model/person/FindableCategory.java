package seedu.address.model.person;

/**
 * A class to represent the categories compatible with find.
 */
public enum FindableCategory {
    COMPANY_NAME,
    APPLICATION_PROCESS,
    POSITION,
    DATE,
    TAGS
}
