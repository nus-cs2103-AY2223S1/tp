package seedu.address.model.module.schedule;

/**
 * Represents the weekday of the slot
 */
public enum Weekdays {
    Monday,
    Tuesday,
    Wednesday,
    Thursday,
    Friday,
    Saturday,
    Sunday
}
