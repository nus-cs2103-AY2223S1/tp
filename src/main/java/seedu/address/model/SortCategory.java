package seedu.address.model;

/**
 * Encapsulates the different sorting categories possible for an entity.
 */
public enum SortCategory {
    ID,
    NAME,
    DEADLINE,
    ISSUE_COUNT,
    URGENCY
}
