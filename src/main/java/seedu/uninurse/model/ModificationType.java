package seedu.uninurse.model;

/**
 * ModificationType is an enum that describes what type of modification is performed on a {@code GenericList}
 */
public enum ModificationType {
    ADD, EDIT, DELETE
}
