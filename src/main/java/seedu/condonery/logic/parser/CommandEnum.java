package seedu.condonery.logic.parser;

/**
 * List of supported commands in Condonery
 */
public enum CommandEnum {
    ADD,
    EDIT,
    DELETE,
    SELECT,
    CLEAR,
    STATUS,
    FIND,
    FILTER,
    RANGE,
    TYPE,
    LIST,
    EXIT,
    HELP,
    UNDO
}
